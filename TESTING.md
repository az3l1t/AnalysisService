# 🧪 Запуск тестов

## Быстрый старт

### Базовый запуск тестов

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запуск всех тестов
pytest

# Или через python модуль
python -m pytest
```

## Различные способы запуска

### 1. Запуск всех тестов

```bash
pytest
```

### 2. Запуск с подробным выводом

```bash
pytest -v
# или
pytest --verbose
```

### 3. Запуск конкретного файла с тестами

```bash
# Юнит-тесты
pytest tests/test_auth.py

# Интеграционные тесты
pytest tests/test_integration.py
```

### 4. Запуск конкретного теста

```bash
# Конкретный класс
pytest tests/test_auth.py::TestPasswordHashing

# Конкретный тест
pytest tests/test_auth.py::TestPasswordHashing::test_hash_password
```

### 5. Запуск с покрытием кода

```bash
# С покрытием в терминале
pytest --cov=app --cov-report=term-missing

# С HTML отчетом
pytest --cov=app --cov-report=html

# После этого откройте htmlcov/index.html в браузере
```

### 6. Запуск с выводом print statements

```bash
pytest -s
```

### 7. Запуск в режиме остановки на первой ошибке

```bash
pytest -x
# или
pytest --exitfirst
```

### 8. Запуск только последних failed тестов

```bash
pytest --lf
# или
pytest --last-failed
```

### 9. Запуск с максимальным выводом ошибок

```bash
pytest -vv
```

### 10. Запуск с отображением локальных переменных при ошибках

```bash
pytest -l
# или
pytest --showlocals
```

## Полезные комбинации

### Полный отчет с покрытием

```bash
pytest -v --cov=app --cov-report=html --cov-report=term-missing
```

### Быстрый запуск с минимальным выводом

```bash
pytest -q
```

### Запуск с выводом всех print

```bash
pytest -s -v
```

### Запуск с остановкой на первой ошибке и подробным выводом

```bash
pytest -x -v
```

## Запуск тестов в Docker

Если вы хотите запустить тесты в контейнере:

```bash
# Запуск тестов в контейнере
docker compose exec auth-service pytest

# Или создать отдельный сервис для тестов
docker compose run --rm auth-service pytest
```

## Структура тестов

```
tests/
├── __init__.py
├── conftest.py          # Конфигурация pytest и фикстуры
├── test_auth.py         # Юнит-тесты для аутентификации
└── test_integration.py  # Интеграционные тесты API
```

## Типы тестов

### Юнит-тесты (test_auth.py)
- Тестирование функций хеширования паролей
- Тестирование создания JWT токенов
- Изолированное тестирование логики

### Интеграционные тесты (test_integration.py)
- Тестирование API endpoints
- Тестирование регистрации пользователей
- Тестирование аутентификации
- Тестирование защищенных endpoints
- Тестирование health check

## Конфигурация

Настройки pytest находятся в файле `pytest.ini`:

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = 
    -v
    --cov=app
    --cov-report=term-missing
    --cov-report=html
```

## Просмотр покрытия кода

После запуска тестов с покрытием:

```bash
# Откройте HTML отчет
open htmlcov/index.html

# Или на Linux
xdg-open htmlcov/index.html
```

## Отладка тестов

### Запуск с отладчиком (pdb)

```bash
pytest --pdb
```

### Запуск с отладочным выводом

```bash
pytest -vv -s
```

## Непрерывное тестирование (watch mode)

Установите pytest-watch:

```bash
pip install pytest-watch
```

Запуск:

```bash
ptw
# или
pytest-watch
```

## Примеры команд

```bash
# Все тесты с покрытием
pytest --cov=app --cov-report=html

# Только юнит-тесты
pytest tests/test_auth.py -v

# Только интеграционные тесты
pytest tests/test_integration.py -v

# Конкретный тест с подробным выводом
pytest tests/test_integration.py::TestUserRegistration::test_register_user_success -vv -s

# Быстрый запуск
pytest -q

# С остановкой на первой ошибке
pytest -x -v
```

## Проверка качества кода

### Запуск линтера (если установлен)

```bash
# flake8
flake8 app tests

# pylint
pylint app tests

# black (проверка форматирования)
black --check app tests
```

## CI/CD

Тесты автоматически запускаются в CI/CD pipeline (см. `.github/workflows/ci-cd.yml`).

## Устранение проблем

### Ошибка: "ModuleNotFoundError"

Убедитесь, что виртуальное окружение активировано:
```bash
source venv/bin/activate
```

### Ошибка: "No module named 'pytest'"

Установите зависимости:
```bash
pip install -r requirements.txt
```

### Тесты не находят модули

Убедитесь, что вы находитесь в корневой директории проекта:
```bash
cd /Users/vardges/Desktop/Микроссервисы
pytest
```

### Проблемы с базой данных в тестах

Тесты используют SQLite в памяти, поэтому PostgreSQL не требуется для запуска тестов.

## Дополнительная информация

- [Документация pytest](https://docs.pytest.org/)
- [Pytest coverage](https://pytest-cov.readthedocs.io/)

