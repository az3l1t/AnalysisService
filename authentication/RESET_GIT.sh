#!/bin/bash

# Скрипт для полного сброса Git репозитория и удаления всех коммитов

set -e

echo "⚠️  ВНИМАНИЕ: Это удалит весь Git репозиторий!"
echo "=============================================="
echo ""
echo "Это действие:"
echo "  - Удалит директорию .git"
echo "  - Удалит всю историю коммитов"
echo "  - НЕ удалит файлы проекта"
echo ""
read -p "Продолжить? (y/n): " CONFIRM

if [ "$CONFIRM" != "y" ] && [ "$CONFIRM" != "Y" ]; then
    echo "Отменено."
    exit 0
fi

echo ""
echo "🗑️  Удаление Git репозитория..."

# Удаление .git директории
if [ -d ".git" ]; then
    rm -rf .git
    echo "✅ Директория .git удалена"
else
    echo "⚠️  Директория .git не найдена"
fi

echo ""
echo "✅ Git репозиторий полностью удален"
echo ""
echo "📋 Следующие шаги:"
echo "   1. Запустите: ./COMPLETE_SETUP.sh"
echo "   2. Или инициализируйте Git вручную:"
echo "      git init"
echo "      git remote add origin https://github.com/Vardges1/medical-analysis-auth.git"
echo "      git add ."
echo "      git commit -m 'Initial commit'"
echo "      git branch -M main"
echo "      git push -u origin main --force"
echo ""

