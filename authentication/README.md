# Микросервис аутентификации и авторизации

Микросервис для аутентификации и авторизации в системе хранения медицинских анализов.

## 🚀 Быстрый старт

1. **Запуск локально**: См. [START_HERE.md](START_HERE.md)
2. **Настройка CI/CD**: См. [YANDEX_CLOUD_QUICK_SETUP.md](YANDEX_CLOUD_QUICK_SETUP.md)
3. **Запуск тестов**: `./run-tests.sh`

## Технологии

- **Python 3.13** - язык программирования
- **FastAPI** - современный веб-фреймворк для Python
- **PostgreSQL** - реляционная база данных (psycopg3)
- **SQLAlchemy** - ORM для работы с БД
- **JWT** - токены для аутентификации
- **bcrypt** - хеширование паролей
- **Docker** - контейнеризация
- **Pytest** - тестирование

## Структура проекта

```
.
├── app/
│   ├── __init__.py
│   ├── main.py          # Точка входа приложения
│   ├── auth.py          # Логика аутентификации
│   ├── database.py      # Настройки БД
│   ├── models.py        # Модели данных
│   └── schemas.py       # Pydantic схемы
├── tests/
│   ├── __init__.py
│   ├── conftest.py      # Конфигурация тестов
│   ├── test_auth.py     # Юнит-тесты
│   └── test_integration.py  # Интеграционные тесты
├── .github/
│   └── workflows/
│       └── ci-cd.yml    # CI/CD pipeline
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий
2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
# Обновите pip и setuptools
pip install --upgrade pip setuptools wheel

# Установите зависимости
pip install -r requirements.txt
```

**Примечание для macOS**: Если команда `pip` не найдена, используйте `python3 -m pip` вместо `pip`.

4. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

5. Запустите PostgreSQL. Есть несколько вариантов:

**Вариант A: Через Docker (рекомендуется)**
```bash
# Используйте скрипт
./start-db.sh

# Или вручную
docker run -d \
  --name auth_db \
  -e POSTGRES_USER=user \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=auth_db \
  -p 5432:5432 \
  postgres:15-alpine
```

**Вариант B: Через Docker Compose (только БД)**
```bash
docker compose up -d db
```

**Вариант C: Локальная установка PostgreSQL**
```bash
# macOS
brew install postgresql@15
brew services start postgresql@15
createdb auth_db
```

6. Запустите приложение:
```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: http://127.0.0.1:8000

**Веб-интерфейс**: Откройте http://localhost:8000 в браузере для доступа к веб-интерфейсу аутентификации.

**Примечание**: Если PostgreSQL не запущен, приложение все равно запустится, но будет показывать предупреждение. Эндпоинты, требующие БД, будут работать после запуска PostgreSQL.

### Docker

Запуск через Docker Compose (современный синтаксис):
```bash
docker compose up -d
```

Для старых версий Docker Compose:
```bash
docker-compose up -d
```

Просмотр логов:
```bash
docker compose logs -f
```

Остановка:
```bash
docker compose down
```

**Примечание**: 
- Если вы получили ошибку "command not found: docker-compose", используйте `docker compose` (без дефиса) - это новый синтаксис для Docker Compose V2.
- Если вы получили ошибку "project name must not be empty", это уже исправлено - в `docker-compose.yml` добавлено явное имя проекта `medical-auth-service`.

Сборка образа:
```bash
docker build -t auth-service .
```

Запуск контейнера:
```bash
docker run -p 8000:8000 -e DATABASE_URL=postgresql+psycopg://user:password@host:5432/db auth-service
```

**Важно**: Перед запуском приложения убедитесь, что PostgreSQL запущен и доступен. См. [QUICKSTART.md](QUICKSTART.md) для подробных инструкций.

## Веб-интерфейс

Приложение включает в себя современный веб-интерфейс для работы с API аутентификации:

- **Главная страница**: http://localhost:8000 - веб-интерфейс для регистрации и входа
- **Регистрация пользователей** - форма для создания нового аккаунта
- **Вход в систему** - форма для аутентификации
- **Управление сессией** - отображение информации о текущем пользователе и токене

Интерфейс полностью функционален и работает с REST API.

## API Endpoints

### Регистрация
```http
POST /register
Content-Type: application/json

{
  "username": "user",
  "email": "user@example.com",
  "password": "password123"
}
```

### Получение токена
```http
POST /token
Content-Type: application/x-www-form-urlencoded

username=user&password=password123
```

### Получение информации о текущем пользователе
```http
GET /users/me
Authorization: Bearer <token>
```

### Health check
```http
GET /health
```

## Тестирование

### Быстрый запуск

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск с покрытием кода
pytest --cov=app --cov-report=html
```

### Дополнительные команды

```bash
# Только юнит-тесты
pytest tests/test_auth.py -v

# Только интеграционные тесты
pytest tests/test_integration.py -v

# Конкретный тест
pytest tests/test_auth.py::TestPasswordHashing::test_hash_password -v

# С остановкой на первой ошибке
pytest -x

# Быстрый запуск (минимальный вывод)
pytest -q
```

**Подробная документация**: См. [TESTING.md](TESTING.md) для полного руководства по тестированию.

**Покрытие кода**: После запуска с `--cov-report=html` откройте `htmlcov/index.html` в браузере.

## CI/CD

Проект настроен для автоматической сборки и деплоя в Яндекс Облако через GitHub Actions.

### 🚀 Быстрая настройка

**Автоматическая настройка** (рекомендуется):
```bash
./setup-yandex-cloud.sh
```

**Ручная настройка**: Следуйте инструкциям в [CI_CD_QUICKSTART.md](CI_CD_QUICKSTART.md)

### Настройка секретов в GitHub

В настройках репозитория (**Settings → Secrets and variables → Actions**) добавьте:

- `YC_SA_JSON_CREDENTIALS` - JSON с учетными данными сервисного аккаунта Яндекс Облака
- `YC_REGISTRY_ID` - ID реестра контейнеров (например: `crp1234567890abcdef`)
- `YC_FOLDER_ID` - ID папки в Яндекс Облаке (например: `b1g1234567890abcdef`)
- `YC_SERVICE_ACCOUNT_ID` - ID сервисного аккаунта
- `DATABASE_URL` - URL подключения к PostgreSQL
- `SECRET_KEY` - секретный ключ для JWT токенов

### Процесс CI/CD

1. **Test** - запускаются тесты при каждом push и pull request
2. **Build and Push** - сборка Docker образа и загрузка в Yandex Container Registry (только для push в main/develop)
3. **Deploy** - автоматический деплой в Yandex Cloud Run (только для push в main)

### 📚 Документация

- [CI_CD_QUICKSTART.md](CI_CD_QUICKSTART.md) - быстрая настройка
- [YANDEX_CLOUD_SETUP.md](YANDEX_CLOUD_SETUP.md) - подробная инструкция
- [DEPLOY.md](DEPLOY.md) - альтернативные варианты деплоя

## Переменные окружения

- `DATABASE_URL` - URL подключения к PostgreSQL
- `SECRET_KEY` - секретный ключ для JWT токенов
- `ALGORITHM` - алгоритм шифрования JWT (по умолчанию HS256)
- `ACCESS_TOKEN_EXPIRE_MINUTES` - время жизни токена в минутах (по умолчанию 30)

## Безопасность

- Пароли хранятся в хешированном виде (bcrypt)
- JWT токены с ограниченным временем жизни
- Валидация входных данных через Pydantic
- Защита от SQL-инъекций через SQLAlchemy ORM

## Документация API

После запуска приложения документация доступна по адресам:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

